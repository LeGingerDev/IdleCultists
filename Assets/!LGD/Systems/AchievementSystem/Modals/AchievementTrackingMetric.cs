public enum AchievementTrackingMetric
{
    Current,      // Track the current value (e.g., "Have 100 gold right now")
    MaxReached,   // Track the highest value ever reached (e.g., "Reach 1000 gold at any point")
    Cumulative    // Track total accumulated over all time (e.g., "Collect 10000 gold total")
}